user_info = []

age = input('Введите возраст пользователя: ')
user_info.append(age)

gender = input('Введите пол пользователя: ')
user_info.append(gender)

name = input('Введите имя пользователя: ')
user_info.append(name)

city = input('Введите город пользователя: ')
user_info.append(city)

klass = input('Введите класс, в котором учится пользователь: ')
user_info.append(klass)

print(user_info)